<div
  id="map"
  style="width: 100%;
  height: 600px;"
  data-markers="<%= @markers.to_json %>"
></div>

<% @users.each do |user| %>
  <h4><%= user.first_name %></h4>
  <p>Skill level: <%= user.skill_level %></p>
<% end %>
